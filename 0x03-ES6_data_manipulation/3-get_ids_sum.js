/**
 * Returns the sum of all student IDs.
 *
 * @param {Array<Object>} students - List of student objects.
 * @returns {Number} Sum of student IDs.
 */
export function getStudentIdsSum(students) {
  return students.reduce((sum, student) => sum + (link unavailable), 0);
}

